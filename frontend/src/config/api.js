// API Configuration
// This will use environment variable if available, otherwise use production URL
export const API_URL = process.env.REACT_APP_API_URL || 'https://turfx.metaqode.co.in/api';

export default API_URL;
